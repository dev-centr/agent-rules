= Project inbox (renamed)
:navtitle: Project inbox
:page-redirect: actor-model-agentic-ui.adoc
:description: Redirect — project inbox was renamed to actor-model agentic UI.

**Project inbox** was renamed to **Actor-model agentic UI**.

The task-grid layout is now one *view* over a persisted *node graph* (discussion nodes spawn task nodes; chat drift spawns child nodes; parent and child record each other's disk addresses).

Go to xref:actor-model-agentic-ui.adoc[Actor-model agentic UI].
