= 2026-08-17 — Thin always-on rules; on-demand skills

== Observed content shift

* Always-on paste (`RULES.md` / later `user.md`) told not to paste skill bodies; long curricula move under `skills/<name>/`.
* Skill directory count jumps as packages land (`tag-release`, `write-skill`, shop skills from prior days stay).

== Skill authoring / naming

* Added `write-skill` (YAML `description` = invocation triggers).
* Renamed package `write-skill` → `write-a-skill` (`065162a`, rename detected in git).
* Always-on file-link Cursor rule so mentioned files open from chat.
